<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <% if params[:plan] == "2"%>
      <h1>Pro Account</h1>
      <p>Sign Up to Game to your hearts content and hosts your own main profile and content</p>
      <%= render "pro_form" %>
    <% else %>
      <h1>Basic Account</h1>
      <p>Sign Up to Game to your hearts content</p>
      <%= render "basic_form" %>
    <% end %>

    <%= render "devise/shared/links" %>

  </div>
</div>
